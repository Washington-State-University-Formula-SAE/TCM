void mode_zero()
{
  // system safety override mode. emergency use only. sensor failure, need to override.

  if () // if the driver pressed the upshift paddle...
  {
    upshift();
  }

  if () // if the driver pressed the downshift paddle...
  {
    
  }

  if () // if the driver wanted to switch out of neutral...
  {
    
  }
}

void mode_one()
{
  if () // if the driver pressed the upshift paddle...
  {
    if (can_upshift())
    {
      upshift();
    }
  }

  if () // if the driver pressed the downshift paddle...
  {
    
  }

  if () // if the driver wanted to switch out of neutral...
  {
    
  }
}

void mode_two()
{
  if () // if the driver pressed the upshift paddle...
  {
    
  }

  if () // if the driver pressed the downshift paddle...
  {
    
  }

  if () // if the driver wanted to switch out of neutral...
  {
    
  }
}
